#include "main.h"

/**
 * _islower - check if the characters are in lowercase
 * Return: 1 if true 0 if false
 */

int _islower(int c)
{
	if (int c == islower)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
